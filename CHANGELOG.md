## Changelog v2.0.0

Looked at the old stack and realized being faster than OPAC doesn't matter if we are ~20x the disk space, so now it’s a **.NET 8 WinForms** thing with rounded cards, Hotkeys still work (F6/F7/Ctrl+Shift+E). If something still looks wrong, assume it was intentional **or** that I was guessing cause both are historically accurate.
